# Generated code-intel cache — the graph/index is rebuilt locally and must not be committed.
# Only config.json (and this .gitignore) are tracked, which is what keeps the project code-intel-wired.
*
!.gitignore
!config.json
